# 真机验收清单

> 一趟跑完约 35 分钟，把 todo.md 里**至今所有还开着的验证**按演示顺序串成一条线：
> 任务 13 / 17 / 21 的旧欠账 + 任务 26 剧本 4a + 任务 27 导演台 v2 + 任务 27.1 故障演练，
> 以及散在各任务记录里的零碎「没验」。
> 手机在手上，笔记本在面前（最好投到大屏），两块屏都要看。
> 最后更新：2026-09-15，对应 master `e7a29bb`（27.1 已部署，线上 `/console/fault-drill` 实测 401 = 路由在）

**这份清单的顺序就是你将来给客户演的顺序**，跑完一遍也等于排练了一遍。
**全程是 Haiku**：之前在 Opus 上验过的几条（改年份、先存预约再建卡、中英马混说）这趟顺带重验。

---

## 开始前（3 分钟）

1. 笔记本开四个标签：
   - **导演台** `https://chatbot.acuventech.com/console?token=<CONSOLE_TOKEN>`（**投到大屏**）
   - **ERP 订单** `https://erp.acuventech.com` → Sales Orders
   - **CRM 看板** `https://crm.acuventech.com/dashboard` → Pipeline
   - **房产后台** `https://chatbot.acuventech.com/vertical-admin`
2. 导演台顶栏是 `Live`；**确认两个开关都是常态**：「Turn tools off (control)」（没变红）、「Break next ERP call (drill)」（没变红）
3. 手机给 **+60 17-394 8123** 发 `menu`，选**零售**

⚠️ **别用后台里现成的老单试退货**——种子发票没有仓库，erp_os 一律拒绝冲红。第 8 步退的必须是第 5 步刚下的那张。

---

## 第一段 · 剧本 1：下单（约 6 分钟）　任务 13 / 21 / 10 / 35.1

| # | 手机上做什么 | 该看到什么 | 不对的话记什么 |
|---|---|---|---|
| 1 | `有什么风扇` | 列表里 **5 款风扇**全在；每行带价格和库存 | 少于 5 款 = 搜索被截断（任务 35.1） |
| 2 | `Boss 这个 earbuds 还有 stock 吗?` | 报 `Sony WF-C710N`、含税价、三个仓库的库存 | 查不到商品 = SKU 被 reseed 掉了 |
| 3 | `要 2 个, 可以 COD 吗?` | 回总价，说明 COD 只限 RM 500 以下、巴生谷 | 答应了 COD = 在编政策 |
| 4 | `算了，改成 3 个` | **重新查库存、重新算价钱** | 直接把上一轮数字乘 1.5 = 没重查 |
| 5 | `确认下单` | 出单号 `SO-...`；**刷新 ERP** 有这张单；**刷新 CRM 看板只多出一张卡**，名字和金额对得上 | ⭐ 没卡 / **两张卡** 都算坏 |
| 6 | `好`（开发票） | 收到 **PDF 发票**，⭐ **点开看一眼**，内容看得清 | 以前只验过「送到了」，没人点开过 |
| 7 | **等 30 秒，什么都别做** | 手机自己响，三语订单确认，金额是 **`RM 986.70`** | `MYR 986.7000` = 格式修复没生效 |

**大屏同步看**（任务 27）：每一步右上都长出业务卡片（放进购物车 / 查库存 / 下单 SO-… / 开票）；第 7 步出现「主动通知客户…（客户没问）」卡；右下后台面板列出 ERP 客户 / 销售单 / 电子发票，标「新增」。**全程屏上不该出现英文长句的给模型的指令。**

---

## 第二段 · 剧本 2a：退货（约 3 分钟）　任务 17

| # | 手机上做什么 | 该看到什么 |
|---|---|---|
| 8 | 拍一张商品照片发过去，配文 `这个坏了，我要退一个` | 认出照片里是什么；跟订单对不上时**反问确认**而不是硬凑。大屏出「看懂了一张图片」卡 |
| 9 | 确认要退刚买的那个，说明原因 | 出退款单号 `CN-...`，金额是单价×1；**ERP 后台能看到那张单** |

---

## 第三段 · 剧本 2b：你自己的文件（约 2 分钟）　任务 17

| # | 手机上做什么 | 该看到什么 |
|---|---|---|
| 10 | 发**你自己的一份 PDF**（跟零售无关最好），问一个只有文件里才有的细节 | ⭐ **答对，底下标 `📄 文件名 · p.N`** |
| 11 | 再问一个**文件里没有的** | 说文件里没提，不编 |

---

## 第四段 · 语音（1 分钟）　任务 36 / 27

| # | 手机上做什么 | 该看到什么 |
|---|---|---|
| 12 | 发一条**中英马混说**的语音，比如「boss，那个 earbuds 我明天 boleh ambil 吗」 | bot 听懂并正常回答；⭐ **大屏「听懂了一条语音」卡里的转写，和你说的逐字对一遍** |

---

## 第五段 · 收尾总结（1 分钟）　任务 19.1

| # | 做什么 | 该看到什么 |
|---|---|---|
| 13 | 导演台点「**End demo · send summary**」 | 手机收到三语总结；**单号和数字对得上刚才真实发生的事**；第 5 步真进了 CRM，所以 **ERP 和 CRM 都被提到**；没写过的后台不许出现 |

---

## 第六段 · 对照组（3 分钟）　任务 13 / 12.2

先发 `menu` 重新选**零售**，开一段新对话，免得编出来的答案混进上面那段历史。

| # | 做什么 | 该看到什么 |
|---|---|---|
| 14 | 导演台点「Turn tools off (control)」→ 手机问 `Sony 那个 earbuds 多少钱，还有几个现货？` | 大屏出红横幅；bot **照样说得很笃定**，但右边**一张工具卡都没有**——这就是要给客户看的「编的」 |
| 15 | 点「Tools off · turn back on」→ 手机**原话再问一遍** | 这次出查商品 / 查库存卡，价格 RM 328.90、库存是 ERP 里的真数（第 5 步刚扣掉 3 个） |

---

## 第七段 · 故障演练 + 人工接管（约 6 分钟）　任务 27.1 / 20 / 21

| # | 做什么 | 该看到什么 |
|---|---|---|
| 16 | 导演台点「**Break next ERP call (drill)**」 | 按钮变红「Drill armed · call off」；卡片流出「Failure drill armed」 |
| 17 | 手机：`我刚才那张单到哪了？` | ⭐ bot **不编单号、不报错**，说系统暂时查不到、转同事。大屏依次：演练提示 → 「System unreachable -- nothing looked up, nothing made up」→ 转人工卡（带模型写的原因）；顶栏变「Human has it」；**按钮自己变回常态** |
| 18 | 手机再发 `在吗` `我等很久了` | ⭐ **一条回复都没有** |
| 19 | 手机发一张**贴纸**（或位置、名片） | ⭐ **同样一条都没有**（以前会冒出「本 demo 只能读文字…」） |
| 20 | 导演台输入框打一句话，Enter | 手机收到，**看不出是人**（没有「客服：」前缀） |
| 21 | 点「**Hand back to bot**」 | ⭐ **手机上不该有任何提示** |
| 22 | 手机再问 `我刚才那张单到哪了？` | bot 正常查到第 5 步那张单；**不提刚才发生过什么** |

**已知会有的方差**：本地测 6 次里 2 次 bot 只说「A colleague will take it from here」、没说系统查不到——得体、不编，但没解释。碰上了记一笔，不算坏。

**可选**（关键词那条路，1 分钟）：发 `转接人工` → 收到三语「帮您转接同事」并静默 → 点交回。关键词表之前两次踩空过，随口换个说法没接住就记下原话。

---

## 第八段 · 剧本 4a：房产看房（约 4 分钟）　任务 26（Flow 仍被 Meta #139000 拦着，演聊天降级版）

发 `menu` 选**房产**。

| # | 手机上做什么 | 该看到什么 |
|---|---|---|
| 23 | `想约看房` | bot 在聊天里要**姓名 / 房源 / 日期**（不会弹表单） |
| 24 | `陈家明 / PROP-202 / 下周六下午3点` | 确认预约；⭐ `/vertical-admin` 出预约、**年份是 2026**；⭐ **CRM 看板出卡**；⭐ **导演台里「保存预约」卡在「建 CRM 线索」卡之前**（串行，不是并发） |

顺带记一下：房产这段每轮回复比零售慢多少（串行多一次往返）。模型要是先写成 2025 被拒，应当**自己改对年份重存**，不回头问客户。

---

## 第九段 · 可选，需要额外的人或写操作

| # | 做什么 | 该看到什么 |
|---|---|---|
| 25 | **三个人同时**给这个号码发消息（各自选零售、各问各的） | 三段对话各自推进、不串台（任务 13 并发验证） |
| 26 | 在 ERP 后台**手工加第 6 款风扇**，手机再问 `有什么风扇` | 出 6 款（任务 35.1）。**这是往 ERP 写数据，你决定做不做** |

---

## 电脑上单独做（不用手机，约 10 分钟）

| 任务 | 做什么 | 该看到什么 |
|---|---|---|
| 24 | 上面任意一段对话进行到一半时**刷新导演台** | 回放一次到齐，**不再卡十几秒才补齐尾巴** |
| 27 | 浏览器窗口拖到 **900px 以下** | 三栏上下堆叠，分隔条消失 |
| 27 | 点「Light background」，**人眼看**浅色观感；**真鼠标拖**两条分隔条、双击复原 | 颜色正常；拖得动、双击回默认、刷新后宽度保留 |
| 27 / 11.2 | 网页聊天 `https://chatbot.acuventech.com` 选**酒店**订一间房、选 **SaaS** 开一张工单 | 导演台出「订房 BK-…」「工单 TCK-…」业务卡片 |
| 11.3 | 网页聊天用**马来语**问一张不存在的单、问一件本店不做的事 | 承认查不到 / 不做，给下一步，不编 |
| 4 | 打开 `https://chatbot.acuventech.com/console/history?token=<CONSOLE_TOKEN>` 抄一个 `conversation_id`，再开 `/console/history/<id>?token=<CONSOLE_TOKEN>` | 聊过两轮以上的对话 `cache_read_tokens > 0`（换 Haiku 后提示词缓存还在命中） |

---

## 要上 VPS 或要你拍板的（不在这一趟里）

| 任务 | 做什么 | 该看到什么 |
|---|---|---|
| 运维 | `grep COMPOSE_FILE /opt/ai_chatbot/.env` | 有 `COMPOSE_FILE=docker-compose.prod.yml` 这一行（防 09-13 那次误拆线上容器） |
| F | erp_os 的 beat 容器里查每晚 3 点重置是否注册 | 调度表里有 `demo-reset-nightly`，或重置历史每晚一行 |
| 34 | 跑一次 `python -m app.tasks.cleanup`（**破坏性，你决定**） | 这趟会留下 `[DEMO]` 卡，CRM 删除计数 > 0；种子联系人不动 |
| 24 | 线上那条错数据：预约 #1 日期 `2025-09-20` 及其 CRM 商机 | 删不删你定 |
| 12 | `deploy/nginx/chatbot.acuventech.com.conf` 是否同步到 `/srv/infra/` | 一致即可，低优先 |
| 24 | 被拒表单的降级补发：临时设 `WHATSAPP_FLOW_ID` + 手机约看房 | 收到三语「请直接回复姓名 / 房源 / 日期」。只有单测覆盖，要改线上配置，低优先 |

## 等外部条件，现在验不了

- **模板消息**（任务 18）：`order_confirmed` / `order_shipped` 批下来后，单独打一枪「24 小时窗口外的推送」
- **原生 Flow 表单**（阻塞项 B / 任务 24）：Business verification 通过、Publish 之后，验表单长相和 `nfm_reply` 真实字段名
- **隐藏手机号的客户**（任务 32）：要等真有这样一个人写进来，验身份路径和用 `recipient` 发消息

---

## 失败了怎么记

每一条失败给我这四样，我能直接定位：

1. **第几步**
2. **你发的原话**（一字不差，标点也算）
3. **手机上收到什么**（截图最好）
4. **导演台上那一刻有什么**（截图；没有任何反应本身也是重要信息）

第 5、13、24 步另外给一张后台截图（CRM 卡 / 总结全文 / 预约）。

---

## 这一趟会在真实系统里留下什么

- **ERP**：1 张销售单、1 张出库单、1 张已验证发票、1 张 DRAFT 退款单
- **CRM**：2 张线索卡（零售一张、房产一张）
- **房产后台**：1 条预约
- 零售的在 `WA-` 开头的 demo 客户名下，CRM 的带 `[DEMO]` 前缀

要清理：ERP 侧 `POST /api/admin/demo-reset`（整表清交易单据，主数据不动）；CRM 侧任务 34 的清理按 `[DEMO]` 标记删。**不清也行**，下次演示多几条历史反而更像真的在用。

## 已知、不用验的

- **网页聊天不执行接管静默**（接管中网页客户再发，bot 照答）：已知 bug，只影响网页线，演示走 WhatsApp 不受影响
- banking 不测，任务 30 下架
