fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic-settings>=2.6,<3.0
anthropic>=0.40,<1.0
httpx>=0.27,<1.0
redis>=5.0,<7.0
# The audit log's three tables live in the shared infra_mysql. Pure Python, so it
# adds no build step to the image, and the driver is all that is needed: the
# schema is three CREATE TABLE statements and the queries are hand-written, which
# is not enough surface to earn an ORM and its migrations.
PyMySQL>=1.1,<2.0
# WXBizMsgCrypt: WeCom encrypts its callback bodies (AES-256-CBC) rather than
# only signing them, so the wecom webhook cannot read a request without this.
cryptography>=43,<47
