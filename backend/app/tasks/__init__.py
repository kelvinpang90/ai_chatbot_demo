"""Jobs that are run against the demo rather than by a customer.

Nothing in here is imported by the service. These are entry points a person
runs -- `python -m app.tasks.cleanup` -- between one demo and the next.
"""
